# Copia este archivo como .env.local y llena los valores (Supabase → Project Settings → API)
VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=tu-llave-publica-anon

# Solo para `npm run db:seed` (nunca en Vercel ni en el repositorio)
SUPABASE_SERVICE_ROLE_KEY=
SEED_PASSWORD=
